import React from "react";
import "./Spinner.css"; // CSS 파일 import

const Spinner = () => {
  return (
    <div className="spinnerContainer">
      <div className="spinner"></div>
    </div>
  ); // 스피너 div 반환
};

export default Spinner;
